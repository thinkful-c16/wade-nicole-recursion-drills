'use strict';

function stringSplitter(str, sep) {
  if (str.length === 0){
    return [];
  }
  else {
    return string.slice()
  }
}

let string = "red/orange/yellow/green/blue/purple";
let separator = "/";